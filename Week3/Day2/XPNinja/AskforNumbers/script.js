let numbers = prompt('Write 2 numbers, separated by commas');
let sum = Number(numbers[0]) + Number(numbers[2]);
console.log(sum);

//works with single digits, doen't work with two-digit numbers etc.
