let colors = ["white", "black", "blue", "red", "pink"];
let number = 1;
for (let i in colors) {
console.log("My #" + number++ + " choice is " + colors[i]);
}

// bonus
 let colors = ["white", "black", "blue", "red", "pink"];
 let number = 1;
 let suffix = ["st", "nd", "rd", "th", "th"];
 for (let i = 0; i < colors.length; i++ ) {
     for (let j = 0; j < suffix.length; j++) {
     }
     console.log("My " + number++ +  suffix[j] + " choice is " +  colors[i]);

 } // don't know, why doesn't work don't understand   
    